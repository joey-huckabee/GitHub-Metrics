# GitHub-Metrics — Documentation

## Start here

| If you want to… | Read |
|---|---|
| Use the tool | [`USER-GUIDE.md`](USER-GUIDE.md) |
| Look up a flag or exit code | [`CLI-REFERENCE.md`](CLI-REFERENCE.md) |
| Understand an error code | [`ERROR-CATALOG.md`](ERROR-CATALOG.md) |
| Know how a metric is calculated | [`METRICS.md`](METRICS.md) |
| Change the code | [`MAINTAINER-GUIDE.md`](MAINTAINER-GUIDE.md) |
| Understand how it fits together | [`ARCHITECTURE.md`](ARCHITECTURE.md) |
| Know what is coming | [`ROADMAP.md`](ROADMAP.md) |

## Requirements

A three-level tree, traced to the test suite. Status is **derived**, never
written down — a document that records its own status will eventually claim
coverage the tests do not provide.

| File | Level | Content |
|---|---|---|
| [`L1.md`](L1.md) | Product | SHALL statements about *what* the tool does, plus non-requirements |
| [`L2.md`](L2.md) | Architecture | *How* each L1 is structurally satisfied |
| [`L3.md`](L3.md) | Implementation | Concrete obligations; where tests attach |
| [`TRACE-MATRIX.md`](TRACE-MATRIX.md) | Generated | Forward trace, artifacts and status |

The matrix is regenerated by `scripts/build-trace-matrix.py` from the
`@pytest.mark.requirement` markers in `tests/`. CI runs it with `--check`, so
it cannot drift from the suite that backs it.

```bash
make trace          # regenerate
make trace-check    # fail if stale
```

## Architecture decisions

Decisions with a real alternative, in [MADR](https://adr.github.io/madr/)
format. Each records the options that were rejected and why — a decision
without its alternatives cannot be reviewed later.

| ADR | Decision |
|---|---|
| [0001](adr/0001-two-column-csv-as-the-inventory-contract.md) | Use a two-column `owner,repoid` CSV as the inventory contract |
| [0002](adr/0002-concurrency-across-files-not-within-a-file.md) | Apply concurrency across inventory files, not within a single file |
| [0003](adr/0003-lenient-ingestion-by-default-with-strict-opt-in.md) | Ingest leniently by default, with strict mode as an opt-in |
| [0004](adr/0004-exit-code-scheme.md) | Use small ordered exit codes rather than sysexits.h |

## The one structural rule

**Ingestion never touches the network. Collection never touches the disk
format.** Neither imports the other; they meet only at the CLI. Most of the
design follows from that, and it is the rule most likely to be broken by a
well-meaning change — see [`ARCHITECTURE.md`](ARCHITECTURE.md).
